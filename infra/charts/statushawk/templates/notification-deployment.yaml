{{- if .Values.notification.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "statushawk.fullname" . }}-notification
  labels:
    {{- include "statushawk.labels" . | nindent 4 }}
    app.kubernetes.io/component: notification
spec:
  replicas: {{ .Values.notification.replicaCount }}
  selector:
    matchLabels:
      {{- include "statushawk.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: notification
  template:
    metadata:
      labels:
        {{- include "statushawk.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: notification
    spec:
      imagePullSecrets:
        {{- toYaml .Values.imagePullSecrets | nindent 8 }}
      containers:
        - name: notification
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          
          command: 
            - celery
            - -A
            - config
            - worker
            - -Q
            - notification_queue
            - --pool=gevent
            - --concurrency={{ .Values.notification.concurrency }}
            - --loglevel={{ .Values.notification.loglevel }}
          
          envFrom:
            - configMapRef:
                name: {{ include "statushawk.fullname" . }}-config
            - secretRef:
                name: {{ include "statushawk.fullname" . }}-secrets
          
          # Health Check: Simple liveness check for Celery
          # (Advanced: use celery inspect ping, but this is simpler)
          livenessProbe:
            exec:
              command: ["/bin/sh", "-c", "celery -A config inspect ping -d celery@$HOSTNAME"]
            initialDelaySeconds: 60
            periodSeconds: 60
            timeoutSeconds: 10
            
          resources:
            {{- toYaml .Values.notification.resources | nindent 12 }}
{{- end }}
