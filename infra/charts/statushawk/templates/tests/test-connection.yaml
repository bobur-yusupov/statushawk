apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "statushawk.fullname" . }}-test-connection"
  labels:
    {{- include "statushawk.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "statushawk.fullname" . }}:{{ .Values.api.service.port }}']
  restartPolicy: Never
