[flake8]
max-line-length = 88
exclude =
    .git,
    __pycache__,
    migrations,
    .venv,
    .env,
    node_modules,
    tests
extend-ignore =
    # Ignore whitespace around brackets (handled by black)
    E203,
    # Ignore line break before binary operator (handled by black)
    W503
